import { FreeConsultationPipe } from './free-consultation.pipe';

describe('FreeConsultationPipe', () => {
  it('create an instance', () => {
    const pipe = new FreeConsultationPipe();
    expect(pipe).toBeTruthy();
  });
});
